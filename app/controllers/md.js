const marked = require('marked')
const fs = require('fs')
const path = require('path')
const ejs = require('ejs')

class Md {
  async getList(ctx) {
    // ctx.body = [
    //   1, 2, 3, 4, 5
    // ]
  }
}

module.exports = new Md()